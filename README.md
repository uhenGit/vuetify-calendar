# Events calendar with Vuetify

### Clone the repo

### Create .env file in the root folder with the next content:
```
VITE_API_TOKEN={your token}
```

### Run the next scripts:

```
npm install
npm run dev
```